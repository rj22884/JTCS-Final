# Copy to deploy_vps.env (gitignored) and adjust if needed.
# Used by JTCS_ERP.bat (Windows) and JTCS_ERP.sh (Ubuntu/Linux).

VPS_HOST=200.141.5.68
VPS_USER=root
VPS_PORT=22
VPS_PATH=/root/JTCS-final

# Public health URL checked from Windows after deploy
PUBLIC_HEALTH_URL=https://app.jtcsxpert.com/health

# Website (jtcsxpert.com) — same VPS user/password as App
# WEB_PATH=D:\JTCS Web Page
# VPS_WEB_DIR=/var/www/jtcsxpert.com
# VPS_WEB_GIT_DIR=/var/repos/jtcsxpert.git
# PUBLIC_WEB_URL=https://jtcsxpert.com
