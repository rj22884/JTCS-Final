# Copy this file to deploy_vps.env and edit values.
# deploy_vps.env is gitignored — never commit real passwords/keys.

VPS_HOST=200.141.5.68
VPS_USER=root
VPS_PORT=22
# Use absolute path (more reliable than ~/JTCS-final over SSH from Windows)
VPS_PATH=/root/JTCS-final

# Optional SSH key (leave empty to type password when prompted)
VPS_SSH_KEY=
