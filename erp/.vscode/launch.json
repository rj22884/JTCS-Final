{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "JTCS ERP (Flask)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "FLASK_RUN_PORT": "8000",
                "PORT": "8000"
            }
        }
    ]
}
